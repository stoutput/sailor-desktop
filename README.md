# sailor-desktop
Container Management for MacOS

### Local Development
Ensure asdf is installed, then:
Run `make` to bootstrap your environment, then run `npm start` to start up a development instance of the app.
